# Hastebin (haste-server) environment configuration
# Copy to app.env and customize before starting

# Timezone
TZ=America/New_York

# Storage backend: redis, file, memcached, postgres, rethinkdb
HASTEBIN_STORAGE=redis

# Paste expiry in seconds (default 30 days)
HASTEBIN_EXPIRE=2592000
